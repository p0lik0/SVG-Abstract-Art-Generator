#ifndef GET_INFO_HPP_ //si la variable de compilation n’est pas connue (d´efinie)
#define GET_INFO_HPP_ //on la d´efinit

#include <iostream>

using namespace std;


string getString(string varNom);

int getInt(string varNom);

#endif //fin des fonctions `a compiler si la variable de compilation n’est pas d´efinie